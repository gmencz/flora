- table:
    schema: public
    name: messages
  insert_permissions:
  - role: guest
    permission:
      check: {}
      columns:
      - content
      - guest_id
      - guest_name
      - nonce
      backend_only: false
  select_permissions:
  - role: guest
    permission:
      columns:
      - content
      - guest_id
      - guest_name
      - id
      - nonce
      - timestamp
      filter: {}
