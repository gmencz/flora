- table:
    schema: public
    name: messages
  insert_permissions:
  - role: guest
    permission:
      check: {}
      columns:
      - content
      - guest_name
      - sent_at
      backend_only: false
  select_permissions:
  - role: guest
    permission:
      columns:
      - content
      - guest_name
      - id
      - message_number
      - sent_at
      filter: {}
      limit: 50
