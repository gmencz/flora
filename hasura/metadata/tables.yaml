- table:
    schema: public
    name: messages
  insert_permissions:
  - role: guest
    permission:
      check: {}
      columns:
      - content
      - guest_name
      - nonce
      backend_only: false
  select_permissions:
  - role: guest
    permission:
      columns:
      - content
      - guest_name
      - id
      - message_number
      - nonce
      - timestamp
      filter: {}
