- table:
    schema: public
    name: messages
  insert_permissions:
  - role: guest
    permission:
      check: {}
      columns:
      - content
      - guest_name
      - id
      backend_only: false
  select_permissions:
  - role: guest
    permission:
      columns:
      - content
      - created_at
      - guest_name
      - id
      - message_number
      filter: {}
      limit: 50
