{
  "name": "chatskee",
  "private": true,
  "workspaces": {
    "packages": [
      "web",
      "gateway",
      "gateway-client"
    ],
    "nohoist": [
      "**"
    ]
  },
  "devDependencies": {},
  "config": {},
  "scripts": {
    "firebase": "firebase emulators:start --import=./.firebase --export-on-exit"
  }
}
