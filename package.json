{
  "name": "chatskee",
  "private": true,
  "workspaces": {
    "packages": [
      "web",
      "gateway",
      "gateway-client",
      "sfu"
    ]
  },
  "scripts": {
    "firebase": "firebase emulators:start --import=./.firebase --export-on-exit"
  }
}
