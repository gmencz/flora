{
  "name": "chatskee",
  "private": true,
  "workspaces": {
    "packages": [
      "tabouleh",
      "knafeh",
      "tahini"
    ],
    "nohoist": [
      "**"
    ]
  },
  "devDependencies": {},
  "config": {},
  "scripts": {
    "firebase": "firebase emulators:start --import=./.firebase --export-on-exit"
  }
}
