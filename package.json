{
  "name": "chatskee",
  "private": true,
  "workspaces": {
    "packages": [
      "web",
      "gateway",
      "gateway-client",
      "sfu"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "firebase": "firebase emulators:start --import=./.firebase --export-on-exit"
  }
}
